# Server Database and API Ports Configuration
# Rename this file to '.env' if you want to override the default local settings.

# Backend Server Listening Port (defaults to 3001 in server.js)
PORT=3001

# MySQL Database Configuration
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=db
